# Datadog configuration for sidecar mode (Gen 2 with serverless-init).
# For compat layer mode (COMPAT_LAYER=true), the deploy script generates .env.gen2.yaml instead.
DD_SERVICE: $DD_SERVICE
PYTHONUNBUFFERED: "1"